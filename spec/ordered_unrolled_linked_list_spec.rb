RSpec.describe OrderedUnrolledLinkedList do
  it "has a version number" do
    expect(OrderedUnrolledLinkedList::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end

  it "test" do
    list = OrderedUnrolledLinkedList::List.new
  end
end
